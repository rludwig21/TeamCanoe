package testing;

import static org.junit.Assert.*;

import java.util.Map;

import org.junit.Test;

import RequestLogger.Database;
import RequestLogger.Search;

public class SearchTEST {
	/**Verifies that search selects the right map corresponding to the search requested
	 * when using the 1 parameter search method
	 */
	@Test
	public void mapSelectionTest() {	
		Search testSearch = new Search();
		Database testDB = Database.getData();
		//select a refinement field
		//verify that the correct map is selected
	}
	
	/**Verifies that search selects the right map corresponding to the search requested
	 * when using the 2 parameter search method
	 */
	@Test
	public void mapSelectionWithRefinementTest() {	
		Search testSearch = new Search();
		Database testDB = Database.getData();
		//select a refinement field
		//verify that the correct map is selected
	}
	/**Verifies that search is actually narrowing results with the keyword
	 * requires that there are logs outside the president's office
	 */
	@Test
	public void refineTest(){
		//select a refinement field
		//enter a search keyword
		//verify that the map is sorted with keyword
		Search testSearch = new Search();
	}
	
}
